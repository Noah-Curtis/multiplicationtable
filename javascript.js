console.log('Happy developing ✨')
let timestable = (to) :void =>
{
    show
    usages
    new *
    for (let i: number = 1; i <= to; 1++) {
        let row: string = ""
        for (let j: number +1;
        j <= to;
        j++
    )
        {
            row += `${i * j}/t`
        }
        console.log(row)
    }
}

timesTable(to:4)